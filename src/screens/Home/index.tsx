import React from 'react';
import { View, Text } from '@ant-design/react-native';
import globalStyles from '@styles/globalStyles';

const Home = () => (
  <View style={globalStyles.containerCenter}>
    <Text style={globalStyles.textLarge}>Home</Text>
  </View>
);

export default Home;
